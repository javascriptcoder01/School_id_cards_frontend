/**
 * Centralized User Roles Constants
 * Exact match with Backend/src/constants/roles.js
 */
export const ROLES = {
  SUPER_ADMIN: 'SUPER_ADMIN',
  COLLEGE_ADMIN: 'COLLEGE_ADMIN',
  OPERATOR: 'OPERATOR',
};

export const ROLE_VALUES = Object.values(ROLES);

export default ROLES;

